# when the container start, redis.conf will take effect immediately.
# more about configure, such as: auth password, you can refer to redis.conf.

APP_REDIS_VERSION=latest
APP_REDIS_PORT=6379
APP_VERSION=latest
APP_PORT=9001
APP_NAME=redis
